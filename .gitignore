# Secrets — never commit (ADMIN_KEY, API keys, etc. live in .env only)
.env
.env.local
.env.*.local
!.env.example

# Downloaded usage exports (may contain phone hashes)
tanya_usage.csv
**/tanya_usage.csv

# Tanya's vault — stays on Railway persistent volume, not in repo
../tanya_brain/

# Usage logs and runtime files
logs/
sync_usage.sh
*.pid
*.sqlite

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# macOS
.DS_Store
